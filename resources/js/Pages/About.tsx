import { PageProps } from '@/types'

interface Props extends PageProps {
  about: {
    content?: string
    images?: string[]
  }
}

const About = () => {
  return (
    <>
      {/* kerjakan di sini */}
    </>
  )
}

export default About